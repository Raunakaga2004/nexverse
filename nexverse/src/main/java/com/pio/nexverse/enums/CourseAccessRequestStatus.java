package com.pio.nexverse.enums;

/**
 * Defines the request status by Department or Employee for course to there owning department
 */
public enum CourseAccessRequestStatus {
    PENDING,
    APPROVED,
    REJECTED
}