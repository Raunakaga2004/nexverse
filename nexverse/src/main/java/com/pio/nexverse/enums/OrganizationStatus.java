package com.pio.nexverse.enums;

/**
 * Defines the current status of the Organization
 */
public enum OrganizationStatus {
    PENDING,
    ACTIVE,
    SUSPENDED // if the organization violates the platform policies
}