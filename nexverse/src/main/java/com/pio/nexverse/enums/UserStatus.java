package com.pio.nexverse.enums;

/**
 * Defines the current status of the User on platform
 */
public enum UserStatus {
    PENDING,
    ACTIVE,
    SUSPENDED // if the user violates the platform policies
}